import { useState } from 'react';
import { PageShell } from '../components/PageShell';
import { PrimaryButton } from '../components/PrimaryButton';

export function ThankYouPage({ onReset }: { onReset: () => void }) {
  const [confirming, setConfirming] = useState(false);
  return <PageShell><section className="rounded-3xl border border-slate-200 bg-white p-7 text-center shadow-card sm:p-10"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-brand-50 text-3xl font-bold text-brand-700" aria-hidden="true">✓</div><h1 className="mt-6 text-3xl font-extrabold text-ink sm:text-4xl">Obrigado por participar!</h1><p className="mt-3 text-lg text-slate-600">Suas respostas foram registradas com sucesso.</p><p className="mt-7 font-bold text-ink">Laboratório de Aprendizado em Robótica — LAR/UERN</p><div className="mt-4 text-sm leading-relaxed text-slate-500"><p>Artemísia Kimberlly — <a className="link" href="mailto:artemisia20261002635@alu.uern.br">artemisia20261002635@alu.uern.br</a></p><p>Maria Klara — <a className="link" href="mailto:mariaklara@alu.uern.br">mariaklara@alu.uern.br</a></p><p>Prof. Dr. Raul Benites Paradeda — <a className="link" href="mailto:raulparadeda@uern.br">raulparadeda@uern.br</a></p></div><div className="mt-9 border-t border-slate-200 pt-7">{!confirming ? <button onClick={() => setConfirming(true)} className="min-h-12 rounded-xl border-2 border-slate-300 px-6 font-bold text-slate-700 transition hover:bg-slate-50 focus:outline-none focus:ring-4 focus:ring-slate-200">Nova participação</button> : <div className="rounded-2xl bg-slate-50 p-5 text-left"><h2 className="text-lg font-bold text-ink">Iniciar uma nova participação?</h2><p className="mt-1 text-sm leading-relaxed text-slate-600">Os dados da participação atual já foram registrados e a aplicação será preparada para o próximo participante.</p><div className="mt-5 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button className="min-h-12 rounded-xl border-2 border-slate-300 px-6 font-bold text-slate-700" onClick={() => setConfirming(false)}>Cancelar</button><PrimaryButton onClick={onReset}>Iniciar nova participação</PrimaryButton></div></div>}</div></section></PageShell>;
}
